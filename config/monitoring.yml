# Copyright (c) 2017 Aimirim STI.

services:

  watchdog:
    container_name: watchdog
    watch_files:
      - ./monitoring.yml

  prometheus:
    container_name: prometheus
    watch_files:
      - ./prometheus.yml
      - ./prometheus_rules.yml

